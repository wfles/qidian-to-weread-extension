# 快速开始指南

## 第一步：准备图标（可选）

为了更好的用户体验，建议准备三个图标文件：
- icon16.png
- icon48.png
- icon128.png

如果没有图标，插件仍然可以正常工作！

## 第二步：安装插件

1. 打开 Chrome 浏览器
2. 在地址栏输入：`chrome://extensions/`
3. 打开右上角的「开发者模式」开关
4. 点击「加载已解压的扩展程序」
5. 选择 `qidian-to-weread-extension` 文件夹
6. 完成！

## 第三步：测试插件

1. 访问起点中文网书籍页面，例如：
   - https://www.qidian.com/book/1042342714/

2. 打开浏览器控制台（按 F12 键）

3. 查看 Console 标签，应该能看到：
   ```
   [起点助手] 插件已加载
   [起点助手] 找到书名: XXX
   [起点助手] 找到作者: XXX
   [起点助手] 开始搜索微信读书...
   ```

4. 如果微信读书有该书，页面上会出现「去微信读书阅读」按钮

## 调试技巧

### 查看日志
所有插件日志都以 `[起点助手]` 开头，方便过滤查看。

### 手动测试
在控制台输入：
```javascript
// 查看获取的书籍信息
console.log(getBookInfoFromQidian());
```

### 常见问题

**问题：图标显示不正常**
解决：这是正常的，不影响功能。可以按照 ICON_README.md 创建图标。

**问题：没有出现按钮**
解决：
1. 检查控制台是否有错误
2. 确认微信读书确实有这本书
3. 确认书名和作者完全匹配

**问题：按钮位置不对**
解决：这可能是因为起点页面结构变化。可以在 content.js 中修改 `insertPositions` 数组。

## 下一步

插件已经可以使用！如果需要调整功能或样式，可以修改对应的文件：
- `content.js` - 主要功能逻辑
- `styles.css` - 按钮样式
- `background.js` - 后台处理

修改后需要在 `chrome://extensions/` 页面点击刷新按钮才能生效。
